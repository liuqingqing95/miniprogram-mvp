<import src="../common/head.xhsml" />
<import src="../common/foot.xhsml" />
<view class="container">
  <template is="head" data="{{title: 'canIUse'}}" />
  <view class="page-body">
    <view class="page-section">
      <input class="weui-input" type="text" bindinput="bindinput"></input>
      <text>返回值：{{content}}</text>
      <button bindtap="click">按钮</button>
    </view>
  </view>
  <template is="foot" />
</view>